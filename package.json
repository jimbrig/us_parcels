{
  "name": "us-parcels",
  "version": "0.1.0",
  "private": true,
  "description": "Geospatial platform for 155M US parcel records",
  "scripts": {
    "serve:map": "bunx serve . -p 8081",
    "verify:map": "playwright test --project=map-only",
    "verify:browser": "playwright test",
    "verify:proxy": "playwright test tests/map-proxy.spec.ts",
    "verify:ui": "playwright test tests/map-visual.spec.ts"
  },
  "dependencies": {
    "@watergis/maplibre-gl-terradraw": "^1.12.1",
    "maplibre-gl": "^5.19.0",
    "pmtiles": "^4.4.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@types/bun": "latest"
  }
}
